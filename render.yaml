services:
  - type: web
    name: collectioncalc-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api_server_v3:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
