# Launch Readiness — Slab Worthy (SINGLE SOURCE OF TRUTH)

> ⚠️ **This file is the durable source of truth for what's left before launch. Status lives HERE — not in a browser window, not as retroactive labels in session notes.** The prior A–F "checklist" existed only in a BO browser tab (never downloaded) and survived as labels in `docs/sessions/WHERE_WE_LEFT_OFF.md`. This doc replaces it. Update this file when readiness status changes.

**Soft launch target:** July 21, 2026 · **GalaxyCon (booth):** Aug 21–23, 2026 · **Launch posture:** gated/batched public beta (`require_approved` + waitlist + beta codes; admit in waves). **Declared HARD gates:** billing end-to-end + valuation/identification honesty.

**MOST RECENT CHANGE (Rule 5):** 2026-07-11 — **Item 2 CONCURRENCY CLUSTER (Phases 1–4) COMPLETE + VERIFIED IN PROD. The single-worker booth-killer found 2026-07-07 is dead.** Phase 3 SHIPPED `901a49e` (billing explicit `finally` on all 9 conn sites + before_request admin flag from the signed JWT claim — one pooled DB checkout saved per authed request; offline 27/27), Phase 4 SHIPPED `820b0ae` (gunicorn `--workers 2 --threads 8 --worker-class gthread` + `.dockerignore`, build context 4.9GB→~450MB, `.env` leak vector closed). **Verification (2026-07-11, post-deploy):** boot log confirms `Using worker: gthread` + 2 workers (pids 7/8); **concurrency probe PASSED** — 12× `/health` hit while an ASM #41 grading request was actively in flight, instant response every time (pre-fix: queued 10–30s); **memory 357.8–358.0MB steady = ~70% of 512MB** (inside the 350–380MB estimate; fallback 1×12 not needed); logs ZERO `POOL EXHAUSTED` / ZERO `[DB]` teardown lines (server-side filtered search, control query validated); pg_stat_activity = **4 connections (1 active) vs max_connections=103**. **Remaining in item 2: (c) Sentry, (d) `/health` DB check, (f) resource self-alert — (f) is the ACTIVE next item** (calibration note for (f): steady-state now sits at ~70% of 512MB, so the placeholder WARN=80% (410MB) leaves only ~52MB of spike headroom — decide sustained-vs-instant threshold at build time). *Prior (2026-07-10 late PM):* **Cover-A/cross-title normalizer fix SHIPPED (`15cb459`) + corpus RE-NORMALIZED + VERIFIED (ebay_sales, 71,449 rows, 0 errors).** Verified against the audit's predictions: stored `is_variant` landed on **exactly 15,344** (2,700 Cover-A rows flipped variant→standard ≈ the predicted 2,601 + rows captured since); **Defenders→Descender cross-title mis-merge = 0 rows remaining**; "Absolute Batman Annual" now its own canonical (42 rows, no longer leaking into the main book); **end-to-end through the live app: AB#1 9.0 → raw FMV $169.99** (`fmv_method=blended`, `estimated=false`, `verdict_reliable=true`) vs the pre-fix $150.00 from the polluted pool. Remaining gap to the ~$185–300 clean-copy market = Layer 3 (grade-aware raw estimate), deferred into R1/R2 by prior decision. **Still open from this arc: the market_sales equivalent pass (8,604 canonical rows, small extension — item 6).** *Prior (2026-07-10 PM):* **eBay account-deletion Issue 1 (compliance timeout) INVESTIGATED + CLOSED (Mike, eBay developer portal): DOWNGRADED from "compliance risk" to low-probability / self-healing / config-confirmed-correct. BOTH eBay endpoint items are now fully closed; neither is a launch-blocker.** Findings: (1) **the actual suspension mechanism is 1000 CONSECUTIVE failures** (server unreachable, or no 200 within 3000ms), and the counter resets on ANY success — not single-timeout fragility; warm endpoint answers in ~0.45s ×3 measured, far under the 3000ms window, so occasional cold-start timeouts cannot plausibly reach 1000 consecutive. (2) **Registered config directly confirmed:** endpoint URL matches prod exactly (`https://collectioncalc-docker.onrender.com/api/ebay/account-deletion`) and the verification token matches Render's env var — stale-config ruled out as a cause of any past timeouts. (3) Data-Handling Compliance bulletin reviewed → **not applicable** (scoped to developers based in CN/RU/KP/CU/IR/VE, eff. Sept 2025; live test notifications show real usernames, e.g. basket924, consistent with unaffected; one-time confirmation that SW's developer account is US-registered = nice-to-have, not urgent). (4) **Bonus independent confirmation of the Issue-2 identity fix:** eBay's own Notifications field-reference documents MARKETPLACE_ACCOUNT_DELETION's identity field as `username` — reading `notification.data.username` targets the documented field. (5) **Free safety net not yet enabled: portal "Notify Me" email on failure detection — RECOMMENDED (2-min, Mike's call)**; eBay would then email proactively rather than us discovering a suspension silently. Keep-warm and the `/health` DB check (item 2(d)) remain nice-to-have hardening — they no longer carry compliance weight. *Prior (2026-07-10, earlier):* **eBay account-deletion Issue 2 (signature verification) SHIPPED (`060f1dc`) + VERIFIED IN PROD — sequence item 3's eBay bullet CLOSED.** All three post-deploy checks passed: (1) unsigned curl POST → **412** `{"error":"Signature required"}`; (2) eBay's own portal "Send Test Notification" fired multiple times → **every one verified successfully**, including across a mid-sequence eBay key rotation (`kid=3cf880e7…` → `kid=9936261a…` — proving the unknown-kid → fresh `getPublicKey` fetch path live, not just the cache), each correctly proceeding to identity lookup and correctly matching no user on eBay's synthetic test IDs; (3) GET challenge-response still 200 with a valid hash. The one thing only a genuinely eBay-signed message could prove — the raw-body byte assumption — is now proven. Same commit also fixed the nested-identity bug (real payloads nest identity under `notification.data`; the old top-level reads meant REAL deletion notices never matched a user). **Remaining on this endpoint: Issue 1 only** (compliance probe timeout — Mike's portal check of eBay's OWN probe status → keep-warm vs monitor-timeout decision; warm probe already proven 200 in ~0.45s ×3). *Prior (2026-07-09):* **Item 2 Phase 1 (shared DB connection pool) SHIPPED + VERIFIED in prod.** `db.py` (ThreadedConnectionPool + PooledConnection proxy, pre-ping, overflow-not-fail, `DB_POOL_DISABLED=1` kill switch) + 8 getter rewires (~59 call sites converted; both cursor flavors preserved; eBay None-contract preserved) + wsgi `teardown_appcontext` leak net. **Verified:** full smoke passed (health/login/my-plan/grade/admin/verify), zero `[DB]` warnings, and a 12-request public-lookup probe showed **zero connection growth** (app parked-set flat at 5 connections; before, every call opened a fresh connection, 4+ per grade). eBay OAuth surface deferred (unrelated extension flakiness that day) — same pool mechanics as the verified surfaces. **NEXT: Phase 2** = the ~75 inline `psycopg2.connect` sites → `db.get_db()` (per-site cursor_factory catalog; migrations/scripts stay raw); **THEN Phase 3** (billing `finally` + drop redundant `before_request` lookup) → **Phase 4** (gunicorn CMD `--workers 2 --threads 8 --worker-class gthread` per measured 173MB RSS on Starter 512MB + `.dockerignore`). Detail in item 2(b) below. *Prior (2026-07-08 PM):* **Billing sequence item 1 FULLY CLOSED. One-diff SHIPPED (`3935ce5`, live 19:42 UTC); core teardown test PASSED on all three fields (`plan=free` / `status=canceled` / `stripe_subscription_id=NULL`), doubly confirmed by an incidental cross-account comparison (user 32 clean vs users 30/31 still carrying pre-fix stale sub_ids); and the add-on test PASSED with BOTH guard branches directly observed in real-time logs after the PYTHONUNBUFFERED deploy: skip branch (canceled non-record dashboard sub → plan unchanged pro/trialing → `ignoring …, not the sub of record` logged) and teardown branch (canceled sub of record → free/canceled/sub_id NULL → `→ free (sub … cleared)` logged). The buffering fix is also confirmed working (logs now visible live). NEXT: sequence item 2 (gunicorn workers/threads + DB pool + finally-closes + Sentry + /health DB check + .dockerignore).** ⚰️ **Implementation tombstone — supersedes item 1's prescription below:** the sub_id-NULL fix WAS done in the helper after all, via a `_UNSET` sentinel default (`billing.py:183`) — omission still means "skip", an explicit `None` now writes SQL NULL; all 5 callers audited (only the deleted handler passes `None`, and it wants NULL). The earlier "targeted direct UPDATE, do NOT touch the helper" instruction is **DEAD — do not re-apply it.** **Observability finding (fixed same day):** `PYTHONUNBUFFERED=1` was MISSING on collectioncalc-docker (L-2026-020 violation), so runtime logs buffered invisibly until container death (proven: a dying container flushed 10 stale `[Billing]` lines with one timestamp at 19:43:17); Dockerfile `ENV PYTHONUNBUFFERED=1` shipped + confirmed working (real-time log lines observed during the add-on test). Discovered while diagnosing a mid-test scare (two post-teardown subs not reflected in DB) — read-only investigation proved **both new subs were DASHBOARD-created**: no `checkout.session.completed` event exists for them, `customer.subscription.created` isn't subscribed/handled, and `invoice.payment_succeeded` is log-only → **all webhook deliveries 200 / 0 failed, service healthy, deploy clean, the billing fix NOT implicated.** The add-on re-run in the correct shape (checkout-created sub of record → dashboard-create a 2nd → cancel the dashboard one → cancel the record one) subsequently PASSED — see top of this entry. NEW post-launch hardening note: dashboard/API-created subs are invisible to the app — paired with the `handle_subscription_updated` last-writer-wins item (Post-launch section). *Prior (2026-07-07 PM):* **Competitive requirements doc received (BS research + Mike), mirrored at `docs/SW_COMPETITIVE_REQUIREMENTS_FOR_DF.md`.** Key updates: (a) **moat framing REVISED** — "only AI raw-comic grader" is no longer true (ComicMintAI, The Comic Locker, Gradr all claim it; none has *proven* accuracy, Gradr publicly panned) → the race is to be the first **demonstrably accurate** AI grader, which makes the grading-accuracy work below the competitive centerpiece, not just hygiene; (b) **R2 side-by-side competitor benchmark added to sequence item 6** (grade the same books through SW + ComicMintAI free tier + known CGC grade — piggyback on the consistency-harness pass, one motion); (c) **⚠️ eBay deletion-endpoint CLARIFICATION (correcting the BS doc's reconciliation, which said the auth-bypass finding "supersedes" the timeout diagnosis): it does NOT — two independent issues on the same endpoint.** The no-signature-verification gap (security) and the probe-timeout diagnosis (eBay-compliance/API-access risk — does the endpoint answer eBay's challenge at all?) BOTH stand; fixing the signature doesn't tell you whether the handler hangs. Do both in one endpoint-touch, as already sequenced. *Earlier same day — full technical review (4-track, read-only) run; findings reconciled into this doc:* (1) **NEW LAUNCH-BLOCKER found: gunicorn runs a SINGLE sync worker** (`Dockerfile:11` — no `--workers`/`--threads`) — every 10–30s grading call serializes the entire API; 2 concurrent booth users = visible outage; compounded by **zero DB pooling** (~130 raw `psycopg2.connect` sites, 4+ fresh connections per grade request) and connection leaks (closes not in `finally`). See launch-critical sequence item 2. (2) **⚰️ STATUS CORRECTION (tombstone): grade retention is BUILT + DEPLOYED** (`grade_retention.py` wired at `routes/grading.py:577-592`, commits `801e79d`+`6fb83f7`; admin surface live) — the "retention spec drafted, unbuilt" framing in this doc and project memory is **DEAD**; only the purge job + pin-on-feedback + `areas_not_visible` persistence remain. (3) Grading consistency NEVER measured (multi-run voting built server-side, frontend hardcodes `runs:1`; live harness exists at root `test_grading_consistency.py --live`); confidence computed/stored but **displayed nowhere**. (4) Security: 3 unauthenticated image-upload endpoints (`routes/images.py`), eBay account-deletion POST acts on attacker-supplied IDs with **no signature verification**, eBay OAuth `state` CSRF. (5) `/health` confirmed to NOT check DB (answers the UptimeRobot design question). (6) AWS Rekognition absent from `dependency_monitor.py` MONITORED_SERVICES — violates the project's own mandatory rule. *Prior (2026-06-29): BILLING hard gate essentially cleared — Model A + teardown verified; one queued diff in `handle_subscription_deleted` (step-3 multi-sub guard + sub_id-NULL, `billing.py:197`); anti-abuse holes logged MEDIUM, coupled to gated signup; eBay deletion-endpoint probe timeout + Stripe SDK 3-majors-behind logged under Dependency watch.*

---

## Lineage (so we don't rebuild blind)
Two overlapping checklists existed; neither was a maintained artifact:
- **~March (Session 59) "47-test QA plan"** — lives in `TODO.md` (Testing sections), `ROADMAP.txt:119`, `CLAUDE_NOTES.txt:62`. Covers Auth, billing, grading, collection, fingerprinting; **~40 of 47 still formally untested.** It ALREADY listed today's "gaps" as untested: **"Mobile testing — full grading flow on real phones"** (`TODO.md:209`) and **"End-to-end grading accuracy test — grade 10+ known-CGC comics, compare — this IS the calibration test suite"** (`TODO.md:294`). **These are long-standing known-untested items, not new.**
- **Spring (Sessions 104–109) "A–F readiness"** — a re-cut that lives only as labels in `WHERE_WE_LEFT_OFF.md` (`A/B early · C=collection mgmt · D=tier gates · E=billing · F=mobile+load`). Most of A–E maps to spring work that was actually done; F was never reached.

This doc supersedes both as the current view. March predates most of spring's work, so status below is CURRENT, not March's.

---

## A–F honest status (no optimism)

| Sec | Area | Status | Detail |
|----|------|--------|--------|
| **A** | (undefined) | ❓ **UNKNOWN — not "done"** | No recorded definition of what A tested ("early" in notes). No artifact, no evidence. Must define what it was meant to cover before claiming complete. |
| **B** | (undefined) | ❓ **UNKNOWN — not "done"** | Same as A — undefined, unverified. |
| **C** | Collection mgmt | 🟡 **RUN — 2 must-fixes OPEN** | Run S104, mostly working (covers load, sort/filter/search, Slab Guard reg, eBay+Whatnot gen, Edit MY VAL all verified). **OPEN:** (1) DELETE (trash icon) has **no confirm/undo** — immediate delete; mobile mis-tap = data loss = trust-breaker. (2) comic-detail row **looks clickable but does nothing** → reads as "broken"; build it or neutralize the affordance. |
| **D** | Tier gates | ✅ **DONE** | Tier Honesty Pass shipped S106 — per-tier grading caps wired to PLANS (Free 25/Pro 100/Guard 250/Dealer 1000), Dealer checkout refused server-side, pricing copy matches product. Server-enforced gates real (extra_photos, slab_guard_regs, vision/extension, signature_id). |
| **E** | Billing | ✅ **Hard gate CLOSED 2026-07-08 — one-diff SHIPPED (`3935ce5`); teardown pass condition MET (all 3 fields) AND both guard branches directly observed in live logs** | Pay→correct tier (incl. Pro→Guard change) verified S108. Stacking guard SHIPPED (`billing.py:525–532`). **Cancel = Model A confirmed 2026-06-29.** **One-diff SHIPPED + VERIFIED 2026-07-08:** (a) **step-3** multi-sub guard — `handle_subscription_deleted` now downgrades only when the deleted `sub.id` matches the stored sub-of-record (falls open on NULL/missing — never traps a paid tier with no live sub); (b) **sub_id-NULL** — `_UNSET` sentinel in `update_user_subscription` (see MOST RECENT CHANGE tombstone). **Pass condition MET, all three fields:** `plan=free` / `status=canceled` / `stripe_subscription_id=NULL`; cross-account confirmation (user 32 clean vs 30/31 pre-fix stale). **Add-on test PASSED (2026-07-08 PM): both guard branches observed in live logs** — skip (non-record cancel → plan unchanged + `ignoring …` line) and teardown (record cancel → full 3-field reset + `→ free (sub … cleared)` line). **Step-2 UX** (account "Change Plan" → portal + 409 auto-redirect) still open, separate — the only billing item left, non-gating. |
| **F** | Mobile + load | 🔴 **NOT STARTED — no checklist exists; load half has a KNOWN pre-existing defect** | The largest un-started gap, and GalaxyCon-critical (booth is phone-first). **Mobile** = full grade→value→verdict→save on real Android+iOS + billing/portal on mobile + PWA install. **Load** = concurrent/convention-spike — the 2026-07-07 review's predicted first failure (**1 sync gunicorn worker + no DB pool**) is ✅ FIXED as of 2026-07-11 (sequence item 2 Phases 1–4 verified: 2×8 gthread + pooling live) — F-load is now UNBLOCKED and would measure real capacity instead of rediscovering the known defect. **No detailed checklist drafted** — first step of F is writing one (devices, flows, a load target). Frontend note for F-mobile: `app.html` is 3,261 lines with ~1,669 lines of inline JS across 10 script blocks alongside `js/` modules — mobile fixes must be found in two places; budget extra time. |

**"A–E complete" is optimistic:** only **D** is cleanly done. C is run-with-open-must-fixes; E is happy-path-verified-but-lifecycle-untested; **A/B are unrecorded.** F is untouched.

---

## Launch-critical sequence (to July 21)

1. ✅ **Billing one-diff — DONE 2026-07-08 (`3935ce5`).** Both fixes shipped in `handle_subscription_deleted`: **step-3** multi-sub guard (downgrade only when the deleted `sub.id` matches the stored sub-of-record; falls open on ambiguous data) **+ sub_id-NULL** (⚰️ implemented via `_UNSET` sentinel in the helper, NOT the direct-UPDATE this item originally prescribed — see MOST RECENT CHANGE tombstone; the sentinel preserves every caller's omission-skips behavior, 5 callers audited). **`--check-db` teardown re-run PASSED on all three fields (`plan=free`/`status=canceled`/`sub_id=NULL`), and the add-on PASSED same day (PM) with both guard branches directly observed in real-time logs** (skip: non-record dashboard-sub cancel → plan unchanged + `ignoring …, not the sub of record`; teardown: record cancel → full 3-field reset + `→ free (sub … cleared)`). **ITEM CLOSED — nothing remains here.** (⚠️ standing rule for any future billing test: throwaway account only, never `test-*`.)
   - **Teardown VERIFIED 2026-06-29** (throwaway sub, immediate-cancel in Stripe sandbox → `customer.subscription.deleted` → handler ran): entitlement teardown **works** — `plan→free`, `status→canceled`, user loses access (the launch-critical part). ✅ Portal cancel = Model A also confirmed (scheduled at-period-end / trial end).
   - **NEW low-sev cleanup found (independent of step-3):** `handle_subscription_deleted` passes `stripe_subscription_id=None` to clear it, but `update_user_subscription` (`billing.py:197`) treats `None` as "field not provided" and **skips the NULL** → the deleted sub_id stays stale on a now-free user. Harmless today (only behavioral reader is the stacking guard `:525`, which is status-gated → free/canceled user still re-subscribes fine; re-subscribe overwrites the stale id; no frontend reads it). Latent trap only: the field "lies" (subscribed-looking while free). **Fix = targeted direct `UPDATE … SET stripe_subscription_id=NULL` in the deleted handler — do NOT flip the helper's None="skip" contract (every caller relies on it).** Independent of step-3; applying step-3 does NOT fix this. LOW severity, not launch-blocking.
2. **CONCURRENCY + OBSERVABILITY HARDENING (added 2026-07-07 review — was LAUNCH-BLOCKING for the booth). ✅ CONCURRENCY CLUSTER (a)+(b)+(e) = Phases 1–4 COMPLETE + VERIFIED 2026-07-11 (see MOST RECENT CHANGE for verification detail). Still open here: (c) Sentry, (d) /health DB check, (f) resource self-alert (ACTIVE next).**
   - ✅ **(a) Gunicorn workers/threads — SHIPPED `820b0ae` + VERIFIED 2026-07-11.** CMD now `--workers 2 --threads 8 --worker-class gthread` (unchanged `--timeout 300`), sized to Starter 512MB (measured post-deploy: 358MB steady ≈ 70%; documented fallback 1×12 not needed). Concurrency probe passed: 12× `/health` instant while a grade was in flight — the 1-sync-worker serialization (one grading call blocking login/collection/health 10–30s; 2 booth users = perceived outage) is gone. Known accepted consequences: in-memory vision rate-limit/daily-cap stores are per-worker → effective caps ×2; gthread `--timeout` is worker-liveness, not per-request kill (long grades safer). Rollback = one-line CMD revert.
   - **(b) DB connection pool + close-in-`finally` — PHASE 1 + PHASE 2 SHIPPED + VERIFIED.** Phase 1 (2026-07-09 AM): `db.py` pool + 8 getters (~59 sites) + wsgi teardown leak-net live; 12-request probe = zero growth, parked set 5. **Phase 2 VERIFIED 2026-07-09 PM (`e75f0f9`, live 21:57 UTC):** all 57 inline `psycopg2.connect` sites → `db.get_db()` (16 files); Mike's smoke passed all surfaces (grade, collection load, admin dashboard, verify lookup, Signatures DB page — no signature match run); DF connection probe = **zero growth** (before=1, after=1 across 12 public-lookup requests, ~0.7s each), **0 `POOL EXHAUSTED` / 0 `[DB]` teardown lines** in Render logs since deploy. Note: parked set SHRANK 5→1 (not the expected growth) — psycopg2's pool parks only `DB_POOL_MIN=1` idle and closes excess on return, and formerly-direct long-lived connections now ride the pool; fewer idle connections is the healthy direction. `db_migrate_*`/`scripts/` deliberately stay raw. ✅ **Phase 3 SHIPPED `901a49e` + VERIFIED 2026-07-11:** explicit `try/finally conn.close()` on all 9 billing.py direct-connection sites (belt over the teardown net; all behavior contracts preserved — offline stubbed-pool suite 27/27) + before_request admin flag now read from the signed JWT `is_admin` claim (verified present, `auth.py:118`) instead of `get_user_by_id()` — drops a pooled DB checkout on EVERY authed request; safety audit: all admin-authorizing routes use `@require_admin_auth` (own fresh DB check), before_request's flag only feeds soft surfaces (vision rate-limit bypass, `is_internal` tag), staleness bound = 30-day token life. ✅ **Phase 4 SHIPPED `820b0ae` + VERIFIED** (see (a)/(e)). **ALL 4 PHASES COMPLETE — this sub-item is CLOSED.** Post-completion prod facts: 4 DB connections (1 active) vs max_connections=103 (3 reserved); pool max 8/worker × 2 workers = 16 + overflow; RSS 358MB/512MB steady.
   - **(c) Sentry** (`sentry-sdk[flask]`, one-line init) — currently NO error tracking, logging is `print()`, no global errorhandler; launch-week debugging would be live-grepping Render logs on a phone. **First slice LANDED 2026-07-08: `ENV PYTHONUNBUFFERED=1` added to the Dockerfile** (+ Render env var) after discovering the service was log-blind — all `print()` output buffered until container death (L-2026-020 violation; even "live-grepping Render logs" was impossible). Sentry itself still open.
   - **(d) `/health` DB check** — `routes/utils.py:23-40` never touches the DB (`SELECT 1` missing), so exactly the failure mode (b) predicts looks green to monitoring. Feeds the UptimeRobot item.
   - ✅ **(e) `.dockerignore` — SHIPPED `820b0ae` (with (a)) 2026-07-11.** Build context 4.9GB → ~450MB (the estimate of "~250MB" was low: tests/ alone was 783MB, `.claude` 1.9GB locally, `.git` 134MB, ComicBookImages/ 100MB); `.env` secret-leak vector closed. Runtime-read audit done before excluding (signature refs come from R2; fonts/JSON/prompts/HTML kept; `comics_pricing.db` = offline scripts only).
   - **(f) (2026-07-09, Mike) Resource-ceiling self-alert — ✅ SHIPPED `bf92fcc` + VERIFIED IN PROD 2026-07-11 (same day its gate was met). ITEM CLOSED.** Verification (all three of Mike's checks passed): live `/api/admin/dependency-status` (via admin JWT) returned the `resources` block — memory 324.7/512MB (63.4%, warn 85), DB 2/100 (2%, warn 70), pool pid 14 checkouts 77 / overflow 0 / leaks 0, streaks 0/3; public `/health` shows ZERO resource lines; code confirmations quoted (dedup prunes resolved keys → recurrence re-alerts; endpoint behind `@require_admin_auth`; monitor DB touches ride `db.get_db()`). **🟡 FOLLOW-UP UNIT DRAFTED IN TREE (2026-07-11, Mike's ask, awaiting review): (i) admin.html resource status chip** — always-visible one-liner under the Dependency Alert banner (Memory / DB / pool, amber when ≥warn or streak building); **(ii) public `/health` MINIMIZED** — was exposing installed Stripe version + versions-behind + monitoring notes to strangers; now returns only `{status, version}` while STILL invoking `check_all()` (the monitor has no cron — health polling IS its scheduler, and the alert email fires inside it; offline-verified 6/6 incl. error-never-leaks); dependency detail + runtime flags (barcode/moderation) moved to `/api/admin/dependency-status`. Files: routes/utils.py, routes/admin_routes.py, admin.html. *(Original build record:)* `check_resources()` — cgroup v2/v1 memory + `pg_stat_activity` vs (max_connections−3) + per-worker `pool_stats()`, 5-min sample TTL, **sustained ×3 samples (~15 min) before warning**; calibration decided at build: WARN=85% memory / 70% DB (env-overridable `RESOURCE_WARN_MEMORY_PCT`/`RESOURCE_WARN_DB_CONN_PCT`) — the 80% placeholder sat only ~52MB above the measured 70% steady (358MB/512MB under 2×8 gthread; DB idle-ish 4/103), so instant-80% would page on GC spikes. Warnings ride the existing DB-persisted state-change email dedup (only the first worker to trip emails); always-visible `resources` snapshot added to `/api/admin/dependency-status`; resource lines FILTERED OFF public `/health` (don't advertise capacity state; email unaffected). MONITORING-ONLY — fallback 1×12 and tier upgrade stay Mike's manual calls, stated in the alert action text. Files: dependency_monitor.py, routes/utils.py, routes/admin_routes.py, CLAUDE.md, ARCHITECTURE.txt. Post-deploy test per dependency rule #4: `/api/admin/dependency-status` → `resources` block shows live numbers. Render Starter has NO native CPU/memory threshold alerts (verified: event-based email/Slack only; threshold alerting needs Pro workspace + OTel metrics stream to an external provider — overkill). Build it into `dependency_monitor.py` instead (merges with the admin Dependency Alert box request): container reads its own cgroup (`/sys/fs/cgroup/memory.current`/`memory.max`) + `pg_stat_activity` count vs ceiling (measured: max_connections=103, 3 reserved) + `db.py` pool counters (per-worker, label as such). WARN ≥80% memory / ≥70% usable DB connections (placeholders, calibrate). Email rides the existing state-change mechanism; line shows in `/api/admin/dependency-status`. **MONITORING-ONLY — no auto-upgrade path anywhere; tier upgrade (Starter→Standard 2GB) stays Mike's manual dashboard decision.** Facts for sizing: instance = Starter 512MB/0.5CPU, measured steady RSS ~173MB single-worker. SEPARATELY (Mike, dashboard-only, 2 min): enable Render native notifications (Settings → Notifications, + Slack if wanted) — event alerts for deploy-fail/unhealthy; meaningful only once (d) sets `healthCheckPath` (currently EMPTY — Render checks port binding only).
3. **🆕 ENDPOINT AUTH GAPS (added 2026-07-07 review — fix before beta admits strangers).**
   - **3 unauthenticated image uploads** — `routes/images.py:48` `/api/images/upload`, `:82` `/upload-for-sale`, `:145` `/submission` have no `@require_auth`; the first two also skip moderation. Anyone can write to R2 → cost abuse + illegal-content hosting. Add auth (or Turnstile + moderation + size cap).
   - ✅ **eBay account-deletion POST — Issue 2 CLOSED 2026-07-10 (`060f1dc`, VERIFIED IN PROD).** POST branch is now verify-first: new `ebay_signature.py` (ECDSA/SHA1 over the RAW body per eBay's scheme; base64-JSON `x-ebay-signature` header; public key via Notification API `getPublicKey` w/ client-credentials app token, cached 1h; tri-state valid/invalid/unavailable → 200/412/500 — 500 makes eBay redeliver, a real GDPR notice is never ack-and-dropped; kill switch `EBAY_SIGNATURE_VERIFICATION_DISABLED=1`). Same commit fixed the nested-identity bug: real payloads nest identity under `notification.data` — the old top-level reads meant REAL deletion notices never matched a user (only forged flat ones could). **Prod verification 2026-07-10, all three checks:** unsigned curl POST → 412 `{"error":"Signature required"}`; portal "Send Test Notification" ×multiple → all verified, incl. across a mid-sequence key rotation (`kid=3cf880e7…`→`9936261a…`, proving the unknown-kid→fresh-key fetch path), each proceeding to identity lookup and matching no user on eBay's synthetic IDs; GET challenge-response still 200. **Issue 1 (compliance probe timeout) also CLOSED 2026-07-10 PM** — downgraded to low-probability/self-healing after Mike's portal investigation (1000-consecutive-failure threshold, config confirmed correct; detail under Dependency watch). **This endpoint carries no remaining launch-blocking work.**
   - Post-launch tier (logged in Post-launch section): eBay OAuth `state` CSRF; `MAX_CONTENT_LENGTH` + image-count cap on `/api/grade`; atomic grading-cap increment.
4. **Section F — mobile (then load).** FIRST **draft the F checklist** (devices, flows, load target — doesn't exist), THEN run on real iOS/Android: full grade→value→verdict→save + billing-on-mobile + PWA install. **Load** second — and only AFTER sequence item 2(a)/(b), or the load test just rediscovers the single-worker defect. This is the biggest gap and the booth depends on it.
5. **C's DELETE-confirm fix** — cheap, data-loss/trust, worst on mobile mis-tap.
6. **Grading-accuracy triage — REVIEW INPUT NOW ON FILE (2026-07-07); decide blocker vs. ship-and-monitor with these facts:**
   - **⚰️ Correction:** retention is **BUILT + DEPLOYED** (contra this doc's prior "spec only" framing) — every grade now persists photos + 8 subgrades + raw_grade + model to `grade_submissions`/R2 with an admin diagnostic surface (`/api/admin/grade-submissions`). **The next matbanshee-style complaint IS diagnosable.** (matbanshee's own grades remain gone forever — retention shipped after.)
   - **The grade is one temperature-0 Sonnet call** (via `call_with_fallback`, `routes/grading.py:459-470`) + deterministic arithmetic (weighted 8-category average, catastrophic-cap, CGC snap). **Multi-run median voting is BUILT server-side (runs 1–3, parallel) but DEAD in prod — frontend hardcodes `runs: 1`** (`app.html:2351`).
   - **Consistency has NEVER been measured.** Historic incident on record: same 4 images → 8.5, 9.2, 8.5 (CLAUDE_NOTES.txt:406, pre-Session-81). ROADMAP's "consistency ✅ RESOLVED (S81)" bought *capability* (structured engine + multi-run), never a *measurement*. **A live harness already exists at root: `test_grading_consistency.py --live --image X --runs 5`** (per-category stdev + grade spread, ~$0.02-0.05/call). Cheapest possible de-risk: run it on 3–5 books.
   - **No Fix-B-style gate on the grade.** "Confidence" is a hardcoded photo-count lookup `{1:65, 2:78, 3:88, 4:94}` (`routes/grading.py:527`) — computed, stored, **displayed NOWHERE** (the warning UI is dead code: `js/grading.js:2157` never called, hidden div `app.html:1208`). A 9.2 from one 401px photo renders in the same gold circle as from 4 crisp photos. The model's `areas_not_visible` honesty signal is parsed and **dropped**. Photo-quality gate (400px + blur floor) checks the **first image only** (`routes/grading.py:419`) — a crisp front + blurry spine grades without complaint = the exact matbanshee old-photo hypothesis.
   - **Recommended pre-launch minimum (≈2 contained sessions):** (i) add `PROMPT_VERSION` to `grading_engine.py` + persist it (L-2026-006 — currently absent, and measurements are meaningless across unversioned prompt edits); (ii) run the consistency harness → get the number; (iii) wire the Fix-B twin: server emits `grade_reliable` (photos<3 or `areas_not_visible` non-empty) + amber "PARTIAL-VIEW ESTIMATE" treatment instead of the gold circle. **Calibration vs 10+ known-CGC books stays the bigger arc** (no ground-truth dataset exists in repo) — post-launch unless the consistency number comes back bad.
   - **🆕 R2 — side-by-side competitor benchmark (from `SW_COMPETITIVE_REQUIREMENTS_FOR_DF.md`, 2026-07-07): piggyback on (ii), one motion.** Grade the same physical books through SW AND ComicMintAI's free tier (2 free credits, no card), against a known/consensus CGC grade where available. Nobody (including competitors) has published this comparison; win = real evidence + booth/ad material, lose = better to know before GalaxyCon. ⚠️ Mind L-2026-005 (announce cost before paid runs — trivial here) and don't create SW-side test-account footguns.
   - **🆕 (2026-07-09, read-only diagnosis — Absolute Batman #1 9.0 → raw FMV $150 vs ~$300 market) MODERN-VARIANT MISPRICING, systemic for multi-cover moderns; upstream of R1/R2 (wrong product → verdict wrong regardless of grade accuracy).** Mechanism, corpus-verified (1,018-row exact-title raw pool, 365d): (a) **`title_normalizer.py:268` flags "Cover A" itself as `is_variant`** → the standard cover's best-labeled sales (156 rows, median $185; clean 1st prints $238–$395) are EXCLUDED from their own book's "standard" estimate; (b) the included pool (456 rows, median $150.00 = the shipped FMV) retains word-form later printings ("Tenth Print" — reprint patterns are numeric-only), Noir editions, artist-name variants ("Jim Lee Cover"), Annual #1 leakage (normalizer canonical collapses "Absolute Batman Annual"→"absolute batman"), a CGC SS slab with graded=false in the raw pool, and lots the shield misses ("lot 1-18", no "#"). (c) Extraction DOES identify printing/cover (vision fields + barcode digits 4/5) but the valuation key is title+issue+issue_type only — identification detail dropped before pricing (same gap class as the parked year/edition finding). Pool is large → Fix-B gate correctly green → **confidently wrong.** **DECIDED (Mike, 2026-07-09 PM): Layers 1+2 = pre-launch, ONE correctness fix (same class as Fix A: data-integrity, not methodology); Layer 3 (grade-aware raw estimate) = DEFERRED into R1/R2 (it changes what the product displays; deserves benchmark framing).** **✅ Fix SHIPPED `15cb459` (2026-07-10) + corpus re-normalized + verified same day (see MOST RECENT CHANGE):** (1) Cover-letter handler — "Cover B+" flags variant, "Cover A" = standard (still stripped from canonical); (2) `_fuzzy_tokens_supported` guard on the 75%-cutoff fuzzy canonical match — every candidate token must be char-supported by the listing text (order/hyphen/typo tolerant BOTH directions: Spiderman↔Spider-Man, Ironman↔Iron Man survive; substituted words fail). **CORPUS AUDIT (whole ebay_sales, 70,986 rows): cross-title leakage is SYSTEMIC — 748 rows / 23 canonicals mis-merged by current code:** absolute batman 611 (Superman/Catwoman line), **descender 56 (= DEFENDERS sales incl. CGC keys!)**, batman adventures 18, uncanny x-men 15 (X-Force), sex criminals 8 (Criminal), fire power 5 (Power Girl), crossed 4 (Crossover), blade 2 (Blaze), killing joke 3 (Killing Time)… **Cover-A: 2,601 corpus rows flip variant→standard** (138 stay variant via other triggers, correctly). **End-to-end AB#1 with both fixes: raw pool median $158.50 → $178.20** (115 cross-title rows leave the pool; remaining gap to ~$300 = Layer 3, deferred). Verified: 10/10 unit sanity + regression (Cover B/C still flag, "Variant Cover A" still flags, fuzzy rescues survive). **ROLLOUT ✅ DONE 2026-07-10 for ebay_sales: dry-run reviewed (computed variant 15,344 vs stored 18,044 = predicted flip), live run 71,449/71,449 updated 0 errors (~1hr from Render shell), post-run verified exact. 🆕 FOLLOW-ON (2026-07-10 late): the market_sales dry-run surfaced a DAY-ONE normalizer bug the new guard exposed — bare leading "new" stripped as a condition prefix eats series names (New Mutants/New Teen Titans/New Warriors…): 1,072 ebay rows affected post-rewrite (635 'Mutants' incl. #98 1st Deadpool; 210 New Teen Titans contaminating the real Teen Titans pool — that class was wrong since day one). Fix SHIPPED + BOTH TABLES RE-NORMALIZED + VERIFIED (2026-07-10 night): ebay re-run 71,449/71,449 + market first run 9,963/9,963, 0 errors both. Post-run checks all exact: leading-New heals landed ('New Mutants' pool = 963 rows, 'Mutants' orphans = 0, New-Teen-Titans-in-Teen-Titans contamination = 0), Cover-A intact (is_variant exactly 15,344 ebay / 221 market), Monstress→Monsters un-merge = 0 remaining. **Live-app proof: New Mutants #98 @9.0 → raw FMV $300, `exact`, high confidence, 10 graded comps** (was stranded in an orphan pool post-rewrite); AB#1 unchanged $169.99 (no regression). **NORMALIZATION LOOP CLOSED — both tables consistent with current normalizer.** Residual tail logged in Post-launch: (a) ~28 rows where real "New *" series fuzzy-merge into parent-name series (New Avengers/Champions/Fantastic Four/Suicide Squad/Excalibur → parents; textually ambiguous, guard can't block subset-candidates; cheap fix = add those 5-7 series to `known_titles.json` so exact match beats fuzzy); (b) media junk in comic pools (3 ASM Blu-ray listings); (c) emoji-led canonicals ('💎 New Mutants Annual', '🔥absolute Batman') fragmenting small pools. Batch all three with the next normalizer touch (word-form printings etc.), none launch-blocking.** Still-separate small items: graded=false CGC slabs in raw pool; word-form printings (tier-2, negligible for AB#1); enumerated-run lot-shield gaps.
   - **Competitive WHY (context for the blocker-vs-monitor decision):** the "only AI raw-grader" moat framing is DEAD (see MOST RECENT CHANGE) — the defensible position is **first demonstrably accurate + honest-about-uncertainty** AI grader. (iii) is therefore also a POSITIONING asset (R3: "the AI grader that tells you when it's unsure" — ComicMintAI's own ToS disclaims its grades; none of the entrants make an honesty claim). R4 confirmed by this review: the confidence-gated slab/no-slab verdict layer (Fix A/B, live since S111) remains unmatched in the researched field.
   - Small bug found in the path: `routes/grading.py:570` references undefined `MONTHLY_GRADING_LIMIT` → swallowed `NameError` → `grading_usage` counter silently missing from responses (likely the logged "0 used usage-meter dead-counter").
7. **New-user flow end-to-end** — overlaps with F-mobile (mobile grade→value→verdict→save IS the new-user flow on a phone). Run together.
8. **Then:** billing step-2 UX redirect; define/verify what A & B were meant to cover (close the unknowns).

---

## Dependency watch (from admin Dependency Alert, 2026-06-29)
- **✅ eBay Account-Deletion (closure) notification endpoint — BOTH ISSUES CLOSED 2026-07-10 (2026-07-07 clarification stands: they were two independent issues, and both got resolved on their own merits).** Issue 1 (original probe timeout): **CLOSED — downgraded to low-probability/self-healing, config confirmed correct** (see closure block at the end of this entry). Issue 2 (2026-07-07 review) — ✅ **CLOSED 2026-07-10 (`060f1dc`, verified in prod; detail in sequence item 3):** POST branch now verifies eBay's ECDSA signature (unsigned → 412; portal test notifications verified incl. across a live key rotation) + nested `notification.data` identity fix. The GET challenge-response endpoint on `collectioncalc-docker.onrender.com` failed our internal monitor with **Read timed out (read timeout=10)**. **Why it matters:** eBay REQUIRES a working account-deletion/closure notification endpoint as a condition of API access (privacy/GDPR). If it genuinely fails **eBay's** compliance probe, eBay can **suspend API access** → kills the sales-data capture pipeline (79K sales = the valuation foundation). **BUT likely just Render cold-start latency** — the rest of the app works, sales are tracking, API cost is live ($8.70/mo), so a 10s timeout may just be catching the endpoint cold. **NEXT-SESSION read-only diagnosis (real-broken vs cold-start, BEFORE any fix):** (a) does it respond correctly when WARM? (b) handler hanging vs spin-up latency? (c) is **eBay's actual** compliance probe passing/failing (vs just our internal monitor)? → if cold-start, fix = keep-warm / raise monitor timeout (not the handler); if real-broken, escalate (API-access risk to the data pipeline). **🆕 (2026-07-09 late) Diagnosis step (a) DONE — endpoint healthy when WARM: 3× GET challenge probe = HTTP 200 in 0.44–0.48s** (correct challengeResponse path, pure sha256 + env read, no DB) — cold-start hypothesis strengthened; handler-hang ruled out warm. **(2026-07-10) Issue-2 fix SHIPPED + verified in the same endpoint-touch (see above) — one supporting data point for Issue 1: eBay's portal test notifications all reached the endpoint and got 200s during verification.** **🏁 ISSUE-1 CLOSURE (2026-07-10 PM, Mike's portal investigation):** (1) eBay's actual suspension mechanism = **1000 CONSECUTIVE failures** (unreachable, or no 200 within 3000ms), counter resets on any success → occasional Render cold-start timeouts cannot plausibly accumulate 1000 consecutive against a warm-path ~0.45s endpoint; the "single timeout = compliance risk" framing is DEAD. (2) Registered endpoint URL + verification token confirmed to match prod/Render exactly — no stale config. (3) Data-Handling bulletin N/A (CN/RU/KP/CU/IR/VE-scoped; one-time US-registration confirmation of the dev account = nice-to-have). (4) eBay's field reference independently confirms `username` as the deletion-notice identity field (validates the Issue-2 `notification.data.username` read). **Keep-warm / monitor-timeout hardening = OPTIONAL nice-to-have now, no compliance weight. OPEN MICRO-ITEM: enable the portal's free "Notify Me" failure-alert email (2-min, Mike) — eBay would proactively email before any suspension instead of silent discovery.**
- **Stripe SDK 3 major versions behind (12.5.1 → 15.3.0) — tech debt, DELIBERATELY post-billing-stabilization, NOT now.** ⚠️ Do NOT upgrade mid-billing-hardening — a 3-major jump risks breaking the exact handlers just verified (`handle_subscription_deleted`, webhook signature verification). **Sequence:** land the billing one-diff (step-3 + sub_id-NULL) → stabilize → THEN a deliberate Stripe upgrade with **full re-test of the cancel/teardown path.**
- *(Informational, no action)* eBay developer **RSS is Akamai bot-protected (HTTP 403)** → API-deprecation news can't be auto-monitored from a server; **manual watch only.** Not actionable.
- **🆕 (2026-07-07) AWS Rekognition NOT in `dependency_monitor.py` MONITORED_SERVICES** (`content_moderation.py` depends on it; monitor lists only Anthropic/eBay/Stripe, `dependency_monitor.py:8-22`) — violates CLAUDE.md's own mandatory third-party rule. Quick fix, do with the next monitor touch.
- **🆕 (2026-07-07) `render.yaml` is STALE** — points at `api_server_v3:app`, which doesn't exist; the live deploy is the Dockerfile (`wsgi:app`). A trap for future debugging ("why isn't render.yaml applied"). Delete or fix.
- **🆕 (2026-07-07) Dependencies are unpinned floors (`>=`) with no lockfile and no CI** — every deploy resolves fresh versions; a bad upstream release (the `anthropic` SDK moves fast) lands in prod unreviewed. Fix: freeze a `requirements.lock` for the Dockerfile + a minimal GitHub Action (install, import `wsgi`, run the 2 real pytest files). Cheap insurance for launch-week hot-fix pushes.

---

## Post-launch / parked (recorded so they're not lost)
- **Cert-number recovery wiring** — the honest marketable slabbed-recovery headline (cert already OCR'd/stored/indexed, just unwired); lane-1, the real recovery upgrade.
- **Valuation normalization tail** — colon/subtitle/accent/token-order title classes (~14 lookups); monitor `lookup_demand` as traffic surfaces them. Plus the **⏰ Fix B gate extension to `very_low`** (exact_thin tier) once we see how often it misleads.
- **Multi-view cross-camera recovery** — the only path to recover LOW-wear raw books (single-image parked, bounded-to-high-wear; SAM + E3 engine retained). See `[[project_slabguard_crosscamera]]`.
- **⚠️ VALUATION DATA COMPLETENESS — page-1-only capture (NOT just a capture-cadence nicety).** The eBay collector extension captures **only the currently-loaded search page** — there is NO auto-pagination (`content.js`: pagination code is read-only display, no `location.href=`/Next-click/`_pgn` advance). Multi-page searches (>240 sold results) capture only page 1's ~240 unless the user manually clicks eBay's native Next. This **systematically under-captures high-VOLUME issues** (popular books span pages) → **caps comp DEPTH on exactly the books that matter** → a concrete mechanism behind the S108 finding ("breadth wide, depth thin; 89% of keys have 1–2 comps") → **degrades verdict accuracy on popular books** (the verdict-accuracy half we fixed in S111). **MITIGATION (decided S111): Mike now manually pages through eBay's native Next on multi-page searches** — closes the gap, human-paced. **Tooling build DROPPED** — the reminder/assist only nudged a manual step Mike will now do anyway; not worth the build. **NO programmatic auto-pagination, ever** (ban risk — see [[feedback_ebay_capture_safety]]). The misleading "looks-clickable-does-nothing" multi-page label is a separate ~5-min someday-polish item (TODO 🟡 Polish), not a real fix.
- **eBay capture tooling / cadence** — capture is manual/bursty; demand-driven depth backfill + variant reclamation (~11K filtered comps).
- **⏰ 90-day grade-retention PURGE** — HARD DEADLINE ~2026-09-17 (published-policy obligation; after launch + GalaxyCon but cannot slip). **(2026-07-07 detail):** `images_purge_after` is written on every row but NOTHING reads it, and **no scheduler exists anywhere in the stack** (no cron in render.yaml, no APScheduler/celery) — the purge job needs a Render Cron Job + one endpoint. **Do pin-on-feedback FIRST or with it:** the `pinned` column exists and admin reads it, but nothing ever SETS it — a complaint filed on day 89 loses its images to the purge. One-line fix in `routes/feedback.py` (pin the user's submissions from the last hour on grading feedback).
- **🆕 Grade-retention completion cluster (2026-07-07)** — retention itself is SHIPPED; three small closures make complaints fully diagnosable: (1) pin-on-feedback (above); (2) persist `areas_not_visible` (computed, returned, dropped — not in the INSERT at `grade_retention.py:58-67`); (3) send the client `grading_id` in the `/api/grade` body + store it, so feedback→submission joins by key instead of the user+timestamp hunt used for matbanshee. Plus `PROMPT_VERSION` column (sequence item 6).
- **🆕 Security post-launch tier (2026-07-07 review)** — (1) eBay OAuth callback trusts `state` as user_id (`routes/ebay.py:124-164`) → OAuth-CSRF lets an attacker link their eBay account to a victim's SW account; use a random session-bound state. (2) No `MAX_CONTENT_LENGTH` / image-count cap — `/api/grade` base64 JSON is unbounded in memory (auth-gated, so post-launch). (3) Grading cap is fail-open + racy (check `:339` and increment `:510` are separate transactions) — make it one atomic `UPDATE … WHERE used < limit RETURNING`; adjacent to but distinct from the logged anti-abuse holes. (4) In-memory per-process rate limits (`monitor.py:98`, `vision.py:73`) silently multiply when workers go >1 — revisit after sequence item 2(a).
- **🆕 Built-but-UNWIRED assets inventory (2026-07-07 review — each is roughly "one page + fetch"):** signature-market analytics suite (`/api/signatures/premium-analysis`, `/signed-sales`, `/db-stats` — signed-vs-unsigned FMV premium, finished, zero frontend refs); barcode scanning (`routes/barcodes.py` + pyzbar in Docker, no caller); extra-photos paid-tier backend (`routes/images.py:225-436`, plan-gated, no UI); Android TWA app (`android-twa/` — gradle + keystore + store listing, a build-and-upload from Play Store); `lookup_demand` gap report (table collecting since S111, no reader); collection CSV export (GET returns full JSON already, export = a serializer). Cert-number recovery stays the headline unwired asset (above).
- **🆕 Small bugs (2026-07-07, none launch-blocking):** `MONTHLY_GRADING_LIMIT` NameError swallowed at `routes/grading.py:570-574` (kills the usage counter — probably the logged "0 used" dead-counter); dead frontend call to nonexistent `/api/cache/update` (`js/app.js:1046`); stale `wsgi.py` docstring (says 11 blueprints, 24 registered); legacy SQLite-backed modules (`reporting.py`, `valuation_model.py`, `scraper.py`, signature v1 JSON db) look like capabilities but are ephemeral-on-Render dead weight — don't count them as live assets.
- **Product-wide confidence-gating audit** — the Fix-B/Slab-Guard lesson generalized: anywhere a low-confidence inference drives a confident product verdict (grade, signature, slab-guard match), gate it.
- **Grades-user re-contact** — gated on (a) improving the grading path AND (b) image retention to diagnose AND (c) a real support inbox. **(2026-07-07): gate (b) is now SATISFIED** (retention shipped); still gated on (a) + (c). Not now, but closer.
- **🛡️ ANTI-ABUSE — free-trial / free-COGS leak (MEDIUM; confirmed S111-cont 2026-06-29; post-launch, NOT launch-blocking for gated beta).** Two compounding holes, both verified in code:
  - **(2) Unconditional repeat trials** — `create_checkout_session` sets `trial_period_days=14` with NO eligibility gate (`billing.py:559`); the stacking guard allows canceled/lapsed users back through (`:519–521`). A user can cancel-before-day-14 → resubscribe → fresh 14-day trial, repeatedly (even on the SAME account/Stripe customer). Fix = trial suppression (check the Stripe customer for a prior subscription → `trial_period_days=0` for returning customers).
  - **(3) Email-alias evasion** — `signup()` normalizes case+whitespace ONLY (`auth.py:653`); duplicate check via `get_user_by_email` (lowercase, `:80`). `doug+1@gmail.com` and `d.oug@gmail.com` evade the dup check → new account → fresh trial. Fix = email normalization at signup (strip `+suffix` broadly; strip dots for gmail/googlemail ONLY — other providers treat dots as significant) before the dup check + store canonical.
  - **Why it matters:** each abused trial grants the tier grading cap (Pro 100 / Guard 250 AI grades/mo) = real Anthropic COGS, not just deferred subscription revenue — the COGS leak is the live concern at any open-signup scale. **Bounded today by the gated/batched beta (beta codes + `is_approved`); grows the moment public signup opens.** **Email normalization is the load-bearing fix** (trial-suppression alone is defeated by alias evasion). Both contained, both post-launch.
  - **🔗 HARD DEPENDENCY — the trigger is the GATE, not a date:** **un-gated public signup MUST NOT be enabled before email-normalization + trial-suppression ship.** Opening public signup IS the event that makes the COGS leak live. Record as: *"before enabling un-gated signup → close anti-abuse holes first."* This is a gating dependency, not a calendar item — if signup stays gated, the holes stay bounded indefinitely.
  - **🎪 GALAXYCON DECISION NEEDED (before Aug 21):** is booth signup **gated** (beta codes / approval) or **open** (QR → instant free signup)? If **OPEN**, these holes become **pre-con fixes** — a concentrated, savvy-collector crowd + free-grading-via-`+N`-emails = word-of-mouth COGS abuse at the worst possible venue. If **gated**, fine, stays parked. **Resolve the booth-signup mode decision before the con** so it's not discovered live.
- **📡 UPTIME MONITORING (UptimeRobot) — LOW priority, pre-launch nice-to-have, LOG ONLY (not built 2026-06-30).** Genuinely wanted before driving real traffic — especially around GalaxyCon (Aug 21–23) when Mike is away from a desk during traffic spikes and needs a phone alert if the site/service goes down. **Design note (so it's not set up naively): monitor MORE than the homepage.** Cover (a) the **public site** (Cloudflare Pages / slabworthy.com) AND (b) the **Render backend via a lightweight health endpoint** — because the grading/valuation API can fail while the static homepage still loads fine, and **Render cold-start/spin-down is a known failure mode here** (cf. the eBay account-deletion endpoint Read-timeout under "Dependency watch"). **Ideal: point UptimeRobot at an endpoint that confirms backend + DB connection alive.** A `/health` endpoint already exists (`collectioncalc-docker.onrender.com/health`, per CLAUDE.md) — **verify it actually checks DB connectivity**; if it only confirms the process is up, extending it (or adding `/api/health` that confirms backend + DB) is part of this task. Free tier covers it.
- **🆕 (2026-07-08) Webhook sub-state sync hardening — TWO paired gaps, post-launch, one touch:** (1) **dashboard/API-created subscriptions are invisible to the app** — the endpoint doesn't subscribe to `customer.subscription.created` and `billing.py` has no handler for it, so a sub created outside Checkout never updates `users` (proven live 2026-07-08: two dashboard subs, DB untouched, every delivered event 200). Users can't trigger this path; it only bites admin/support actions. (2) **`handle_subscription_updated` is customer-matched last-writer-wins** (`plan or 'free'` from event metadata — a metadata-less sub's update writes `plan='free'` over a real tier). Fix both in one pass: subscribe + handle `.created` via the updated-handler, match on sub-of-record like the deleted-handler now does, and never downgrade plan from absent metadata.
- Lower-priority: ~30s comic-ID progress messaging; email setup (mike@/support@); Signatures v2 (detection gate, CORS server-fetch); resilience gap (7/12 model call sites lack `call_with_fallback`); CGC cost-sourcing audit; admin Feedback comment truncation; "0 used" usage-meter dead-counter; Dealer webhook hardening.

---

## Provenance
Assembled read-only 2026-06-27 (Session 111) from: `WHERE_WE_LEFT_OFF.md` (A–F labels + session detail), `TODO.md` (P0–P5 + Testing sections, the 47-test plan), `ROADMAP.txt`/`CLAUDE_NOTES.txt` (Session 59 lineage), and direct code verification of `routes/billing.py` (stacking guard `:525–532`, deletion handler `:797–818`). No fixes performed. Keep this file current; it is the source of truth.

**2026-07-07 update:** reconciled findings from the full 4-track read-only technical review (grading-accuracy deep-dive, architecture/code-health, security/perf/scalability, feasibility inventory) run at Mike's request. Retention-shipped correction verified against git (`801e79d`, `6fb83f7`). All file:line refs verified in code at review time. No fixes performed; Mike to convert findings into requirements docs.
